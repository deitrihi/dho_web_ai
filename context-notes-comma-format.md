# 숫자 콤마 텍스트 컬럼 정제 컨텍스트 노트

## 이전 세션 플랜 파일 관계
모바일 반응형 작업 때 쓰던 `plan.md`/`checklist.md`/`context-notes.md`는
`plan-mobile-responsive.md` 등으로 이름을 바꿔 보존함(체크리스트 중 "실제 브라우저 육안
확인"만 미완으로 남아있었음, 삭제하지 않음). 이번 파일들은 숫자 콤마 정제 작업 전용.

## 조사 방식과 근거
전체 `dho_structured.sqlite3`의 TEXT 컬럼을 정규식 `^-?[0-9]{1,3}(,[0-9]{3})+(\.[0-9]+)?$`로
스캔해서 콤마 포함 순수 숫자 값을 가진 테이블.컬럼을 전수 확인(에이전트 위임). 이후 직접
`raw_attrs`를 cannon/recipe/consumable/tarotCard(handcrafted 4개 카테고리) 대상으로 같은
패턴으로 재스캔해서, `materialize_cannon.py`류 손으로 짠 스크립트 중 실제로 콤마 영향을
받는 라벨이 `cannon.관통력` 하나뿐임을 확인함(추측 대신 실측으로 범위 확정).

## 설계 결정
- **화면 출력 지점 재확인**: 처음엔 webapp 템플릿에 콤마 포맷 필터를 붙이는 걸 가정했지만,
  `dho_webapp.py:106-109` 주석에 파생 테이블(cannon 등 211개)을 웹앱이 아예 안 읽는다고
  명시돼 있고 실제 `item_detail`도 `raw_attrs`/`raw_tables` 원문만 렌더링함을 확인. 원문은
  원본 사이트 그대로라 이미 콤마가 있으므로 건드릴 필요 없음. 숫자를 실제로 "표기"하는
  유일한 지점은 챗봇 `run_sql` 결과 JSON — 사용자에게 확인 후 이 경로로 확정.
- **관계 테이블 컬럼명**: `{header}_text` 컬럼이 전부 숫자로 판별되면 `{header}_num`으로
  컬럼명을 바꾸기로 함(타입과 이름 불일치 방지). 이 컬럼명을 하드코딩 참조하는 곳이
  webapp/chat/openwebui 어디에도 없음을 grep으로 확인(전부 `SELECT *` 또는
  `get_table_schema`로 동적 조회) — 이름 변경해도 회귀 없음.
- **run_sql 포맷 적용 범위**: 모든 int/float 값에 콤마를 붙이면 `item_id` 같은 식별자도
  "12,345"로 바뀌어 수량처럼 오해될 수 있음 -> `item_id`/`row_index`/`position`과
  `*_id` 접미사 컬럼은 포맷에서 제외하기로 결정(이 스키마 전체에서 FK/PK 컬럼이 전부
  이 명명 규칙을 따름을 확인했으므로 안전한 휴리스틱).
- **materialize_cannon.py 절단 버그**: 콤마 문제와 별개로 발견됐지만 같은 원인(정규식이
  콤마를 처리 못함)이라 같이 고치는 게 자연스럽고, 사용자도 승인함.

## 재생성 후 검증 결과
- 214개 테이블 전부 재생성 전/후 행 수 일치(데이터 유실 없음, 백업 DB와 대조 확인).
- `courtRank.명성`/`discovery.발견 경험치`/`ganador.공격력`/`research.필요 페이지`/
  `portPermit__명성.*_num`/`ship__기본 성능.*_num` 등 전부 콤마 없는 INTEGER로 정상 전환.
- `cannon.penetration` 절단 버그: item_id 8868/8869/8912/8927/8967을 raw_attrs 원문
  ("1,032" 등)과 대조해 정상값(1032 등)으로 복구됐음을 확인.
- `run_sql`(Python)/`runSql`(TS) 양쪽에서 실제 SELECT 실행 결과, `item_id`는 그대로,
  `penetration`/`명성`은 `"1,500"` 형태 문자열로 반환되는 것 확인.
- webapp: Flask test_client로 `/`, `/cannon`, `/cannon/8868`, `/assistant` 200 확인.
- chat: `npm run lint`, `npm run build` 통과.

## 부수 발견 (이번 범위 밖, 수정 안 함)
- `city__판매 아이템.발전도_text`/`투자_text`는 재생성 후에도 여전히 TEXT + 콤마 포함
  상태로 남음. 원인: `build_relation_tables`가 라벨이 같은 "판매 아이템" 표를 여러 도시
  페이지에서 모아 헤더 합집합으로 컬럼을 만드는데, 이 표의 "발전도"/"투자" 헤더가 실제로는
  두 가지 다른 의미(숫자 투자 금액 vs "필요"라는 플래그 텍스트)로 섞여 쓰이고 있어서 해당
  헤더 전체가 순수 숫자로 판별되지 않음. 이건 원본 데이터 자체가 같은 헤더명 아래 다른
  종류의 값을 담고 있는 것이라, 임의로 숫자만 남기고 "필요" 값을 버리면 정보 손실이 생김
  -> 안전하게 TEXT로 유지(콤마 정제 로직은 올바르게 동작 중, 회귀 아님). 나중에 필요하면
  이 표만 손으로 분리하는 별도 작업으로 처리 가능.

## 미해결 / 범위 밖
- `materialize_recipe.py`/`materialize_consumable.py`의 qty 파싱(`re.search(r"\d+", ...)`)도
  이론상 콤마가 있으면 같은 버그가 나겠지만, 실측 스캔 결과 현재 데이터에는 콤마 케이스가
  없어서 이번 범위에서 제외(요청 범위 밖 예방적 수정 지양).
- `tarot_card`/`tarotCard` 테이블명 불일치는 여전히 미해결(이전 세션부터 알려진 이슈, 이번
  작업과 무관).
