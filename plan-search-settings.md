# 검색 결과 대분류 순서 설정 페이지

## 배경
검색 결과(`/search`)는 카테고리별로 묶여 나오는데, 지금은 매칭 건수(`COUNT(*) DESC`)
순으로만 정렬됨. 사용자 요청: webapp에 설정 페이지를 만들고, 검색 결과에서 카테고리를
묶는 대분류(모험/아이템/선박/인물·스킬/NPC/세계) 순서를 사용자가 직접 정할 수 있게
하는 메뉴를 추가.

## 접근 방식
- 새 테이블 `search_group_order(group_title TEXT PRIMARY KEY, sort_order INTEGER)` —
  사용자가 설정 페이지에서 저장한 대분류 순서만 담는다. 값이 없으면(최초 상태)
  `category_localization.group_order`(사이드바 내비게이션과 동일한 기본 순서)로
  폴백 — 별도 초기화 없이 바로 동작.
  - 이 테이블은 크롤링→구조화 파이프라인과 무관한 앱 설정값이라
    `DERIVED_PIPELINE_SCRIPTS`(재크롤링 시 재생성되는 파생 테이블)에 넣지 않고,
    `postgres/init.sql`(크롤링 데이터 이관용 부트스트랩과 무관, extension/wikidb만
    다룸)도 안 건드림 — `dho_webapp.py` 모듈 로드 시 `CREATE TABLE IF NOT EXISTS`로
    자체적으로 스키마를 보장(기존 프로젝트에 "설정값 저장용 테이블" 사례가 없어
    직접 판단, 크롤링 데이터 테이블과 섞이지 않게 분리).
- 정렬 UI는 드래그앤드롭 JS 라이브러리 없이 ▲▼ 버튼(각각 순수 POST 폼)으로 구현 —
  대분류가 6개뿐이라 드래그앤드롭 없이도 충분하고, 프로젝트에 이미 있는 "폼 제출 →
  리다이렉트" 패턴과 일치.
- `get_search_results()`는 카테고리를 그룹으로 묶어 정렬하되, **그룹 순서는 설정값,
  그룹 내부는 기존처럼 매칭 건수 내림차순**을 유지(사용자가 처음 질문에서 본
  "대분류 순서 + 그 안에서 건수순" 옵션을 기본값으로 채택 — 별도 요청 없었지만
  합리적인 기본값으로 판단).
- 네비게이션(상단바)에 "설정" 링크 추가.

## 범위 밖
- 드래그앤드롭 UI, 다른 설정 항목(색상 테마 등) 추가 안 함.

## 추가 요청 (2026-08-11) — 대분류 하위 소분류(카테고리) 순서도 설정
- 새 테이블 `search_category_order(category TEXT PRIMARY KEY, sort_order INTEGER)` —
  `search_group_order`와 동일한 오버레이 패턴. 카테고리가 속한 대분류는
  `category_localization`에서 조인해서 알아낸다(중복 저장 안 함).
- **기존 기본 동작(매칭 건수 내림차순)을 깨지 않기 위한 핵심 결정**: 대분류
  하나라도 소분류 순서를 커스터마이징하면 그 대분류 안에서는 커스텀 순서를
  전적으로 따르고, 아직 손대지 않은 대분류는 지금처럼 매칭 건수 내림차순을 유지한다.
  즉 대분류별로 "커스텀 고정 순서" 아니면 "건수순" 둘 중 하나 — 부분 커스텀(일부
  카테고리만 순서 지정, 나머지는 건수순 섞임)은 안 함, 사용자가 예측하기 쉬운
  단순한 모델을 유지하기 위함.
- 설정 페이지에서 대분류를 아코디언으로 펼치면 그 안의 소분류(카테고리) 목록이
  ▲▼ 버튼과 함께 나온다. 소분류를 한 번도 안 건드린 대분류는 `category_localization`의
  `order_in_group`(사이트 기본 순서)을 초기 표시값으로 보여준다 — 버튼을 누르는
  순간 그 대분류가 "커스텀"으로 전환된다.
