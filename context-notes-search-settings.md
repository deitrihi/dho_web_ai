# 검색 결과 대분류 순서 설정 페이지 — 작업 로그

## 2026-08-11 | 시작

사용자에게 검색 결과 카테고리 정렬 기준 3가지(대분류 순서만/대분류+건수순/가나다순)를
물었더니, 셋 다 아니고 "webapp에 설정 페이지를 만들고, 검색 결과의 대분류 순서를
정하는 메뉴를 넣어줘"로 답함 — 정렬 기준 자체가 아니라 **사용자가 직접 정하는 설정
UI**를 원함.

**결정: 새 전용 테이블(`search_group_order`) + `postgres/init.sql`/파생 테이블
파이프라인 건드리지 않음.** `category_localization`(대분류 매핑)은
`migrate_to_postgres.py`가 1회성으로 옮긴 원본성 테이블이라 사용자가 이걸 직접
편집하게 만들면 원본 데이터 레이어와 사용자 설정이 섞임 — 대신 "커스텀 설정값이
있으면 그걸 쓰고, 없으면 category_localization 기본 순서로 폴백"하는 별도 오버레이
테이블로 분리함.

**Why:** category_localization은 원본 사이트 JS 번들에서 그대로 옮겨온 "사실
데이터" 레이어(`build_category_localization.py` 주석 참고)로 취급돼 왔음 — 여기에
사용자 개인 설정을 얹으면 그 원칙이 깨짐. 별도 테이블이면 나중에 재크롤링/데이터
갱신이 있어도 사용자가 정한 순서는 안전하게 유지됨.

**How to apply:** `search_group_order`는 크롤링 파이프라인 스크립트
(`DERIVED_PIPELINE_SCRIPTS`)에 안 넣음 — 항목 저장 때마다 재생성되면 사용자 설정이
날아감. `dho_webapp.py` 모듈 로드 시 `CREATE TABLE IF NOT EXISTS`로 자체 스키마
보장.

**정렬 기준 기본값**: 그룹 순서(설정값) 우선, 같은 그룹 안에서는 매칭 건수
내림차순 — 첫 질문에서 사용자에게 보여준 두 번째 옵션("대분류 순서 + 그 안에서
건수순")을 별도 확인 없이 합리적 기본값으로 채택. 사용자가 다른 기준을 원하면
후속 조정.

## 2026-08-11 | 구현 및 검증 완료

UI는 드래그앤드롭 없이 ▲▼ 버튼(순수 POST 폼, JS 불필요)으로 구현 — 대분류가 6개뿐이라
충분하고 프로젝트에 이미 있는 "폼 제출 → 리다이렉트" 패턴과 일치시킴.

**검증**: 기본 상태(설정 없음)에서 카테고리 순서가 category_localization 순서를
따르는 것 확인 → 설정 페이지에서 "세계" 그룹을 맨 위로 이동 → `/search?q=대포` 결과에서
세계 그룹 소속 카테고리("이벤트")가 실제로 맨 앞으로 이동하는 것 확인(정렬 로직이
올바르게 동작). 145개 라우트 전체 스모크 테스트 통과. Playwright로 설정 페이지
버튼 클릭 → 순서 변경 반영 시각 확인. 로컬 컨테이너 재빌드 후 실제 응답으로 재검증,
테스트 중 변경한 순서는 배포 전 기본값으로 리셋. NAS(`./deploy.sh webapp`) 배포 후
`/settings` 응답으로 실제 반영 확인.

## 2026-08-11 (이어서) | 대분류 하위 소분류 순서 추가

사용자 요청: "대분류 하위에 소분류도 설정할 수 있으면 좋겠어."

**핵심 결정: 대분류 단위로 "커스텀 고정 순서" 아니면 "건수순" 둘 중 하나만 — 부분
커스텀 안 함.** 소분류 하나만 순서를 옮겨도 그 소분류가 속한 대분류 전체가
"커스터마이징됨" 상태로 전환되고, 그때부터 해당 대분류 안에서는 매칭 건수와 무관하게
저장된 순서를 그대로 따른다. 아직 손 안 댄 대분류는 이전과 동일하게 건수 내림차순.

**Why:** 커스텀 순서와 건수순을 섞으면(예: 순서 지정된 카테고리는 앞에, 안 된
카테고리는 건수순으로 뒤에) 사용자가 왜 이 카테고리가 여기 있는지 예측하기 어려워짐.
대분류 단위의 단순한 on/off 모델이 예측 가능성 면에서 더 낫다고 판단(별도 확인 없이
결정 — 필요하면 후속 조정 가능).

**How to apply:** `search_category_order`에 그 대분류 소속 카테고리가 하나라도 있으면
`get_customized_search_groups()`가 해당 대분류를 "커스텀"으로 잡고,
`get_search_results()`가 건수순 대신 저장된 순서를 씀. `category_localization`(원본
데이터)과 마찬가지로 이 테이블도 별도 오버레이라 재크롤링에 영향 안 받음.

**검증**: "아이템" 그룹의 마지막 소분류(아이템샵)를 맨 위로 이동 → `/search?q=장`
전체 순서 인덱스 기준 21번째 → 14번째(= "모험" 그룹 블록이 끝나고 "아이템" 그룹
블록이 시작하는 바로 그 위치)로 이동 확인. 동시에 손대지 않은 "모험" 그룹 블록
(인덱스 0~13)은 완전히 그대로임을 확인 — 대분류별 on/off 모델이 의도대로 동작.
145개 라우트 스모크 재통과, Playwright로 아코디언 펼침/접힘 및 렌더링 확인,
NAS 배포 완료.
