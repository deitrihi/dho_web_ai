# 2단계 — 구조화 DB 스키마 설계 및 파싱

## 목표
`dho_cache.sqlite3`(원본 HTML 캐시, 34,198페이지)에서 데이터를 뽑아 카테고리별 전용 테이블을
가진 구조화 DB(`dho_structured.sqlite3`)를 만든다. 최종 목표는 로컬 LLM이 Text-to-SQL로
질의할 수 있는 스키마이므로, 카테고리별로 컬럼명이 명확한 전용 테이블을 만든다
(사용자 확정: [[schema-design-no-erd-reference]] 참고, EAV 방식 대신 전용 테이블 선택).

## 접근 방식
1단계로 원본 HTML → (범용) 중간 표현으로 뽑는 **범용 파서**를 만들고, 2단계로 카테고리별
"매핑 정의"를 통해 중간 표현을 최종 전용 테이블로 옮긴다. 70개 카테고리마다 처음부터
BeautifulSoup 파싱 코드를 새로 짜지 않기 위함 — 사이트 전체가 일관된 "속성-값 행 + 표"
구조를 쓰는 것을 샘플 분석(tarotCard/cannon/recipe/consumable/cityNpc/quest/ship)으로 확인함.

- **범용 추출**: 상세 페이지 HTML → `{title, attrs: [(label, text, links)], tables: [(label, headers, rows)]}`
  형태로 파싱하는 함수 하나. 카테고리 무관하게 전체 70개/33,496건에 적용 가능.
- **스테이징 테이블**: 범용 추출 결과를 그대로 저장 (`raw_attrs`, `raw_tables`). 재실행 시
  원본 HTML을 다시 파싱하지 않고 여기서부터 이어갈 수 있음.
- **카테고리별 매핑**: 각 카테고리마다 "이 label은 이 컬럼, 이 표는 이 관계 테이블"이라는
  선언적 매핑을 작성해 스테이징 데이터를 최종 전용 테이블로 pivot. 이 부분만 카테고리마다
  작성이 필요하지만, HTML 파싱보다 훨씬 적은 코드.

## 범위 (이번 작업)
70개 카테고리를 구조적으로 비슷한 그룹으로 나눠서 진행한다 (전부 한 번에 끝내지 않고
카테고리 그룹 단위로 검증하며 확장). 그룹 분류와 진행 순서는 `checklist.md` 참고.

## 산출물
- `dho_structured.sqlite3` — 카테고리별 전용 테이블 + 공용 관계 테이블
- `build_structured_db.py` (가칭) — 범용 추출기 + 카테고리 매핑 실행 스크립트
- 파싱 실패/누락 로그 (검증용)

## 리스크
- 카테고리마다 속성 label이 미묘하게 다를 수 있음 (오탈자, 공백) → 매핑 시 실제 데이터로 검증
- quest처럼 자유 텍스트가 많은 카테고리는 완전한 정규화가 어려울 수 있음 → 텍스트 컬럼으로 남기고
  구조화는 명확한 필드(보상/필요조건 등)만 진행
